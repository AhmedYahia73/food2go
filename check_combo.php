<?php require __DIR__.'/vendor/autoload.php'; $app = require_once __DIR__.'/bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $addon = \App\Models\Addon::where('name', 'combo')->first(); echo 'Addon ID: ' . $addon->id . "\n"; echo 'Name: ' . $addon->name . "\n"; echo 'Tax ID: ' . $addon->tax_id . "\n"; if ($addon->tax_id) { $tax = \App\Models\Taxe::find($addon->tax_id); echo 'Tax Name: ' . $tax->name . "\n"; } else { echo 'No tax_id on addon.\n'; }
